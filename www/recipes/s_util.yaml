less: >-
  a { margin: 10px }

recipes:
  prevnext:
    contents:
      - div:
          - a:
              if: s => s.data.links[0]
              href: 's1.html'
              textContent: first
          - span:
              else: true
              textContent: first
          - a:
              if: s => s.data.links[1]
              href: s => s.data.links[1]
              textContent: next
          - span:
              else: true
              textContent: next
    data:
      links: [true,false]
  
