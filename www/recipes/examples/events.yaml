---
html:
  body:
    - h1: Hello Events
    - button:
        calculate:
          textContent: s => 'clicked ' + s.data.get('count',0) + ' times'
        on_click: (s,ev) => s.data.set( 'count', 1 + s.data.get( 'count' ) )
        
