---
html:
  body:
    contents:
      - button:
          calculate:
            textContent: s => 'clicked ' + s.data.get('count',0) + ' times'
          events:
            click: (s,ev) => s.data.set( 'count', 1 + s.data.get( 'count' ) )
        
