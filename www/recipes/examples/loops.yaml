---
page:
  body:
    - div:
        - h1: 
            foreach: s => [ "world", "galaxy", "universe" ]
            forvar: where
            textContent: s => 'hello ' + s.it.where + '(' + s.idx.where + ')'
