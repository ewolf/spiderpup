title: example

page:
  body:
    - testapp:

recipes:
  testapp:
    data:
      count: 0
    contents:
      - button:
          textContent: s => s.data.count
          on_click: s => s.data.count++
